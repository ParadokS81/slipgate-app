export default function SettingsTab() {
  return (
    <div class="flex flex-col items-center justify-center h-full text-center opacity-40">
      <div class="text-4xl mb-3">⚙️</div>
      <h2 class="text-lg font-semibold mb-1">Settings</h2>
      <p class="text-sm max-w-xs">
        Discord account, notification preferences, and app configuration will live here.
      </p>
    </div>
  );
}
